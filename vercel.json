{
  "builds": [
    {
      "src": "/backend/app.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/" },
    { "source": "/api/(.*)", "destination": "/backend/app.js" }
  ]
}
